# edit vim directory and track changes with chezmoi on exit

emulate -L zsh

cd "${XDG_CONFIG_HOME}/nvim/lua"
nvim chadrc.lua +NvimTreeToggle
chezmoi add --recursive .
chezmoi apply -k
cd -

# vim: ft=zsh
