# interactively search dirstack, prefering tmux popup when possible

emulate -L zsh

local dirb=$(dirs -v |  cut -d'	' -f2 | fzf-tmux -p 60%)

if ! [[ -z $dirb ]]; then
  BUFFER="cd ${dirb}"
  zle accept-line
fi

# vim: ft=zsh
