# interactively search dirstack, prefering tmux popup when possible

emulate -L zsh

local dirb=$(hash -d | column -s= -t | fzf-tmux -p 50%,30%  | awk '{print $2}')

if ! [[ -z $dirb ]]; then
  BUFFER="cd ${dirb}"
  zle accept-line
fi

# # vim: ft=zsh
